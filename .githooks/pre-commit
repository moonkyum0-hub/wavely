#!/bin/sh
# 커밋 직전에 타입 체크 + lint + 테스트를 강제 실행.
# 실패하면 커밋이 중단된다. 급할 때는 git commit --no-verify로 건너뛸 수 있지만,
# CI가 어차피 같은 검사를 돌리므로 결국 고쳐야 한다.
echo "pre-commit: npm run check 실행 중..."
npm run check || {
  echo ""
  echo "❌ 검사 실패 — 커밋이 중단됐어요. 위 에러를 고친 뒤 다시 커밋해주세요."
  exit 1
}
